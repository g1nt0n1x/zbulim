#!/usr/bin/env bash
# zbulim (Albanian: reconnaissance/discovery)
# automated recon: SMB enum → /etc/hosts → TCP sweep → targeted sCV → UDP sweep → targeted sUCV
# author: g1nt0n1x
# https://github.com/g1nt0n1x/zbulim

set -euo pipefail

# ── Colors ────────────────────────────────────────────────────────────────────
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
CYAN='\033[0;36m'
BOLD='\033[1m'
DIM='\033[2m'
RESET='\033[0m'

# ── Banner ────────────────────────────────────────────────────────────────────
banner() {
    echo -e "${CYAN}${BOLD}"
    echo "  ███████╗██████╗ ██╗   ██╗██╗     ██╗███╗   ███╗"
    echo "  ╚══███╔╝██╔══██╗██║   ██║██║     ██║████╗ ████║"
    echo "    ███╔╝ ██████╔╝██║   ██║██║     ██║██╔████╔██║"
    echo "   ███╔╝  ██╔══██╗██║   ██║██║     ██║██║╚██╔╝██║"
    echo "  ███████╗██████╔╝╚██████╔╝███████╗██║██║ ╚═╝ ██║"
    echo "  ╚══════╝╚═════╝  ╚═════╝ ╚══════╝╚═╝╚═╝     ╚═╝"
    echo -e "${RESET}${DIM}         zbulim — recon automation by g1nt0n1x${RESET}"
    echo
}

# ── Helpers ───────────────────────────────────────────────────────────────────
info()    { echo -e "${BOLD}${CYAN}[*]${RESET} $*"; }
success() { echo -e "${BOLD}${GREEN}[+]${RESET} $*"; }
warn()    { echo -e "${BOLD}${YELLOW}[!]${RESET} $*"; }
fail()    { echo -e "${BOLD}${RED}[-]${RESET} $*"; }
section() { echo -e "\n${BOLD}${CYAN}── $* ──${RESET}"; }

alert() {
    local color="$1"; shift
    local title="$1"; shift
    echo
    echo -e "${color}${BOLD}  ┌──────────────────────────────────────────────────────┐${RESET}"
    printf "${color}${BOLD}  │ %-52s │${RESET}\n" "$title"
    echo -e "${color}${BOLD}  ├──────────────────────────────────────────────────────┤${RESET}"
    for line in "$@"; do
        printf "${color}${BOLD}  │${RESET} %-52s ${color}${BOLD}│${RESET}\n" "$line"
    done
    echo -e "${color}${BOLD}  └──────────────────────────────────────────────────────┘${RESET}"
    echo
}

# Extract and display only the nxc result line ([+] or [-])
nxc_result() {
    local output="$1"
    local stripped
    stripped=$(echo "$output" | sed 's/\x1b\[[0-9;]*m//g')
    local line
    line=$(echo "$stripped" | grep -P '\[\+\]|\[-\]' | head -1) || true
    if [[ -z "$line" ]]; then
        return
    fi
    if echo "$line" | grep -qP '\[\+\]'; then
        echo -e "  ${BOLD}${GREEN}${line}${RESET}"
    else
        echo -e "  ${BOLD}${RED}${line}${RESET}"
    fi
}

usage() {
    echo -e "${BOLD}Usage:${RESET}"
    echo "  zbulim <target> [output-dir]"
    echo
    echo -e "${BOLD}Arguments:${RESET}"
    echo "  target      IP address or hostname to scan"
    echo "  output-dir  Directory for scan results (default: ./nmap)"
    echo
    echo -e "${BOLD}Phases:${RESET}"
    echo "  1. nxc smb   — host enum, null/guest auth, signing, shares, users"
    echo "  2. TCP sweep — full port scan, then targeted -sCV on open ports"
    echo "  3. UDP sweep — top-1000 scan, then targeted -sUCV on open ports (sudo)"
    echo
    echo -e "${BOLD}Examples:${RESET}"
    echo "  zbulim 10.10.10.1"
    echo "  zbulim 10.10.10.1 ./nmap"
    echo "  sudo zbulim 10.10.10.1       # enables UDP scans"
    exit 0
}

extract_open_ports() {
    local gnmap_file="$1"
    local proto="$2"
    grep -oP "\d+(?=/open/${proto})" "$gnmap_file" | paste -sd, || true
}

elapsed() {
    local secs=$(( $(date +%s) - START_TIME ))
    printf "%dm%02ds" $(( secs / 60 )) $(( secs % 60 ))
}

# ── Entry point ───────────────────────────────────────────────────────────────
[[ $# -lt 1 || "$1" == "-h" || "$1" == "--help" ]] && banner && usage

TARGET="$1"
OUTDIR="${2:-nmap}"
START_TIME=$(date +%s)
TCP_PORTS=""
UDP_PORTS=""
NULL_AUTH=false
GUEST_AUTH=false

banner
info "Target  : ${BOLD}${TARGET}${RESET}"
info "Output  : ${BOLD}${OUTDIR}/${RESET}"
info "Started : $(date '+%Y-%m-%d %H:%M:%S')"

mkdir -p "$OUTDIR"

# ═══════════════════════════════════════════════════════════════════════════════
# SMB Enumeration — nxc
# ═══════════════════════════════════════════════════════════════════════════════
section "SMB Enumeration"

if ! command -v nxc &>/dev/null; then
    warn "nxc not found — skipping SMB enumeration phase."
else
    # ── Host discovery + /etc/hosts ───────────────────────────────────────────
    info "Enumerating target via SMB..."
    NXC_INIT=$(nxc smb "$TARGET" --generate-hosts-file "${OUTDIR}/hosts" 2>&1) || true

    if [[ -s "${OUTDIR}/hosts" ]]; then
        HOSTS_ENTRY=$(cat "${OUTDIR}/hosts")
        success "Hosts entry: ${BOLD}${HOSTS_ENTRY}${RESET}"

        if grep -qF "$TARGET" /etc/hosts 2>/dev/null; then
            warn "Target already present in /etc/hosts — skipping."
        else
            if sudo tee -a /etc/hosts < "${OUTDIR}/hosts" > /dev/null 2>&1; then
                success "Appended to /etc/hosts"
            else
                warn "Could not append to /etc/hosts. Run manually:"
                echo -e "  ${DIM}cat ${OUTDIR}/hosts | sudo tee -a /etc/hosts${RESET}"
            fi
        fi
    else
        warn "nxc returned no host info — SMB may not be available."
    fi

    # ── Null session ──────────────────────────────────────────────────────────
    info "Testing null session..."
    NXC_NULL=$(nxc smb "$TARGET" -u '' -p '' 2>&1) || true
    nxc_result "$NXC_NULL"

    if echo "$NXC_NULL" | grep -qP '\[\+\]'; then
        NULL_AUTH=true
        alert "$YELLOW" \
            "NULL SESSION AUTHENTICATED" \
            "Anonymous access is allowed on this target." \
            "Shares, users, and further enum will use null creds."
    fi

    # ── Guest login ───────────────────────────────────────────────────────────
    info "Testing guest login..."
    NXC_GUEST=$(nxc smb "$TARGET" -u 'guest' -p '' 2>&1) || true
    nxc_result "$NXC_GUEST"

    if echo "$NXC_GUEST" | grep -qP '\[\+\]'; then
        GUEST_AUTH=true
        alert "$YELLOW" \
            "GUEST LOGIN AUTHENTICATED" \
            "Guest account is active on this target." \
            "Shares, users, and further enum will use guest creds."
    fi

    # ── SMB signing ───────────────────────────────────────────────────────────
    info "Checking SMB signing..."
    NXC_SIGN=$(nxc smb "$TARGET" --gen-relay-list "${OUTDIR}/signing-off.txt" 2>&1) || true

    if [[ -s "${OUTDIR}/signing-off.txt" ]]; then
        alert "$YELLOW" \
            "SMB SIGNING NOT REQUIRED" \
            "Target is vulnerable to SMB relay attacks." \
            "Saved to: ${OUTDIR}/signing-off.txt"
    else
        info "SMB signing is required — relay not possible."
    fi

    # ── Share + User enumeration (only if null or guest works) ────────────────
    if $NULL_AUTH || $GUEST_AUTH; then
        section "Share + User Enumeration"

        if $NULL_AUTH; then
            info "Enumerating shares via null session..."
            nxc smb "$TARGET" -u '' -p '' --shares > "${OUTDIR}/shares-null.txt" 2>&1 || true
            SHARE_COUNT=$(grep -cP 'READ|WRITE' "${OUTDIR}/shares-null.txt" || true)
            success "Found ${BOLD}${SHARE_COUNT}${RESET} accessible shares — saved to ${OUTDIR}/shares-null.txt"

            info "Enumerating users via null session..."
            nxc smb "$TARGET" -u '' -p '' --users --users-export "${OUTDIR}/users-null.txt" > /dev/null 2>&1 || true
            if [[ -s "${OUTDIR}/users-null.txt" ]]; then
                USER_COUNT=$(wc -l < "${OUTDIR}/users-null.txt")
                success "Exported ${BOLD}${USER_COUNT}${RESET} users — saved to ${OUTDIR}/users-null.txt"
            else
                warn "No users returned via null session."
            fi
        fi

        if $GUEST_AUTH; then
            info "Enumerating shares via guest login..."
            nxc smb "$TARGET" -u 'guest' -p '' --shares > "${OUTDIR}/shares-guest.txt" 2>&1 || true
            SHARE_COUNT=$(grep -cP 'READ|WRITE' "${OUTDIR}/shares-guest.txt" || true)
            success "Found ${BOLD}${SHARE_COUNT}${RESET} accessible shares — saved to ${OUTDIR}/shares-guest.txt"

            info "Enumerating users via guest login..."
            nxc smb "$TARGET" -u 'guest' -p '' --users --users-export "${OUTDIR}/users-guest.txt" > /dev/null 2>&1 || true
            if [[ -s "${OUTDIR}/users-guest.txt" ]]; then
                USER_COUNT=$(wc -l < "${OUTDIR}/users-guest.txt")
                success "Exported ${BOLD}${USER_COUNT}${RESET} users — saved to ${OUTDIR}/users-guest.txt"
            else
                warn "No users returned via guest login."
            fi
        fi
    fi
fi

# ═══════════════════════════════════════════════════════════════════════════════
# TCP
# ═══════════════════════════════════════════════════════════════════════════════
section "TCP — Full Port Sweep"
info "Running full TCP scan (all 65535 ports)..."

nmap -p- --min-rate 10000 -oA "${OUTDIR}/tcp-allports" "$TARGET"

TCP_PORTS=$(extract_open_ports "${OUTDIR}/tcp-allports.gnmap" tcp)

if [[ -z "$TCP_PORTS" ]]; then
    warn "No open TCP ports found."
else
    success "Open TCP ports: ${BOLD}${TCP_PORTS}${RESET}"

    section "TCP — Targeted Version + Script Scan"
    info "Running -sCV on: ${TCP_PORTS}"

    nmap -p "$TCP_PORTS" -sCV -oA "${OUTDIR}/tcp-targeted" "$TARGET"

    success "TCP targeted scan complete."
fi

# ═══════════════════════════════════════════════════════════════════════════════
# UDP (root only)
# ═══════════════════════════════════════════════════════════════════════════════
section "UDP — Full Port Sweep"

if [[ $EUID -ne 0 ]]; then
    warn "UDP scans require root — skipping."
    info "Re-run with: ${BOLD}sudo zbulim ${TARGET} ${OUTDIR}${RESET}"
else
    info "Running UDP scan (top 1000 ports)..."

    nmap -sU --min-rate 5000 --open -oA "${OUTDIR}/udp-allports" "$TARGET"

    UDP_PORTS=$(extract_open_ports "${OUTDIR}/udp-allports.gnmap" udp)

    if [[ -z "$UDP_PORTS" ]]; then
        warn "No open UDP ports found."
    else
        success "Open UDP ports: ${BOLD}${UDP_PORTS}${RESET}"

        section "UDP — Targeted Version + Script Scan"
        info "Running -sUCV on: ${UDP_PORTS}"

        nmap -p "$UDP_PORTS" -sUCV -oA "${OUTDIR}/udp-targeted" "$TARGET"

        success "UDP targeted scan complete."
    fi
fi

# ═══════════════════════════════════════════════════════════════════════════════
# Summary
# ═══════════════════════════════════════════════════════════════════════════════
echo
echo -e "${GREEN}${BOLD}┌─────────────────────────────────────────────────────────┐${RESET}"
echo -e "${GREEN}${BOLD}│                     Scan Complete                       │${RESET}"
echo -e "${GREEN}${BOLD}└─────────────────────────────────────────────────────────┘${RESET}"
echo -e "  ${DIM}Target   :${RESET} ${BOLD}${TARGET}${RESET}"
[[ -n "${TCP_PORTS}" ]]  && echo -e "  ${DIM}TCP      :${RESET} ${TCP_PORTS}"
[[ -n "${UDP_PORTS}" ]]  && echo -e "  ${DIM}UDP      :${RESET} ${UDP_PORTS}"
[[ -f "${OUTDIR}/hosts" ]] && echo -e "  ${DIM}Hosts    :${RESET} $(cat "${OUTDIR}/hosts")"
$NULL_AUTH  && echo -e "  ${DIM}Null Auth:${RESET} ${BOLD}${GREEN}Yes${RESET}"
$GUEST_AUTH && echo -e "  ${DIM}Guest    :${RESET} ${BOLD}${GREEN}Yes${RESET}"
[[ -s "${OUTDIR}/signing-off.txt" ]] && echo -e "  ${DIM}Signing  :${RESET} ${BOLD}${YELLOW}Not Required${RESET}"
echo -e "  ${DIM}Output   :${RESET} ${OUTDIR}/"
echo -e "  ${DIM}Elapsed  :${RESET} $(elapsed)"
echo
